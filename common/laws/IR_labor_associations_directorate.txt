law_voluntary_association = {
	group = lawgroup_labour_associations
	parent = law_combination_acts

	icon = "gfx/interface/icons/law_icons/voluntary_association.dds"

	progressiveness = 0

	is_visible = {
		has_law_or_variant = law_type:law_directorate
	}

	disallowing_laws = {
		law_regulatory_bodies
		law_worker_protections
		law_sovereign_monopoly
	}

	on_activate = {
		if = {
			limit = {
				has_law_or_variant = law_type:law_right_to_associate
			}
			custom_tooltip = trade_unions_much_more_radical_effect_tt
		}
		if = {
			limit = {
				has_law_or_variant = law_type:law_corporatized_unions
			}
			custom_tooltip = trade_unions_more_radical_effect_tt
		}
		if = {
			limit = {
				has_law_or_variant = law_type:law_corporatized_unions
			}
			custom_tooltip = trade_unions_less_corporatist_effect_tt
		}
		if = {
			limit = {
				OR = {
					has_law_or_variant = law_type:law_regulatory_bodies
					has_law_or_variant = law_type:law_worker_protections
				}
			}
			activate_law = law_type:law_no_workers_rights
		}
	}

	modifier = {
		state_political_strength_from_wealth_mult = 0.25
		building_minimum_wage_mult = -0.1
		state_expected_sol_from_literacy = -2
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}

		# AI should really try to get out of Guild System under the right circumstances
		if = {
			limit = {
				has_law_or_variant = law_type:law_guild_system
				NOT = { has_strategy = ai_strategy_reactionary_agenda }
				OR = {
					NOT = { has_strategy = ai_strategy_conservative_agenda }
					any_political_movement = {
						law_enactment_stance = {
							law = law_type:law_combination_acts
							value > neutral
						}
						political_movement_support >= 0.10
					}
				}
			}
			add = 1000
		}
	}
}

law_internal_arbitration = {
	group = lawgroup_labour_associations
	parent = law_corporatized_unions

	icon = "gfx/interface/icons/law_icons/internal_arbitration.dds"

	progressiveness = 0

	is_visible = {
		has_law_or_variant = law_type:law_directorate
	}

	unlocking_technologies = {
		corporatism
	}

	disallowing_laws = {
		law_polycentric_law
		law_worker_protections
	}

	on_activate = {
		if = {
			limit = {
				has_law_or_variant = law_type:law_combination_acts
			}
			custom_tooltip = trade_unions_less_radical_effect_tt
		}
		if = {
			limit = {
				OR = {
					has_law_or_variant = law_type:law_guild_system
					has_law_or_variant = law_type:law_anti_strike_laws
					has_law_or_variant = law_type:law_combination_acts
					has_law_or_variant = law_type:law_right_to_associate
				}
			}
			custom_tooltip = trade_unions_more_corporatist_effect_tt
		}
		if = {
			limit = {
				has_law_or_variant = law_type:law_worker_protections
			}
			activate_law = law_type:law_regulatory_bodies
		}
	}

	modifier = {
		country_authority_add = 100
	}

	institution = institution_corporate_unions
	institution_modifier = {
		state_pop_support_movement_labor_mult = -0.05
		state_radicals_and_loyalists_from_sol_change_mult = -0.1
		building_minimum_wage_mult = -0.02
	}

	ai_will_do = {
		any_interest_group = {
			is_in_government = yes
			OR = {
				has_ideology = ideology:ideology_corporatist
				leader ?= {
					OR = {
						has_ideology = ideology:ideology_bonapartist
						has_ideology = ideology:ideology_fascist
						has_ideology = ideology:ideology_integralist
						has_ideology = ideology:ideology_corporatist_leader
					}
				}
			}
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

law_corporate_peonage = {
	group = lawgroup_labour_associations
	parent = law_national_labor_service

	icon = "gfx/interface/icons/law_icons/corporate_peonage.dds"

	progressiveness = -50

	unlocking_technologies = {
		macroeconomics
	}

	is_visible = {
		has_law_or_variant = law_type:law_directorate
	}

	disallowing_laws = {
		law_polycentric_law
		law_regulatory_bodies
		law_worker_protections
	}

	unlocking_laws = {
		law_sovereign_monopoly
	}

	on_activate = {
		if = {
			limit = {
				OR = {
					has_law_or_variant = law_type:law_worker_protections
					has_law_or_variant = law_type:law_regulatory_bodies
				}
			}
			activate_law = law_type:law_no_workers_rights
		}
	}

	on_enact = {
		custom_tooltip = close_the_borders_tt
		if = {
			limit = {
				has_law_or_variant = law_type:law_right_to_associate
			}
			custom_tooltip = trade_unions_much_more_radical_effect_tt
		}
		if = {
			limit = {
				has_law_or_variant = law_type:law_corporatized_unions
			}
			custom_tooltip = trade_unions_more_radical_effect_tt
		}
		if = {
			limit = {
				has_law_or_variant = law_type:law_corporatized_unions
			}
			custom_tooltip = trade_unions_less_corporatist_effect_tt
		}
	}

	modifier = {
		state_lower_strata_expected_sol_add = -2
		state_middle_strata_expected_sol_add = -1
		interest_group_ig_trade_unions_pol_str_mult = -0.5
		country_authority_add = 200
		state_migration_pull_mult = -0.25
		country_institution_cost_institution_corporate_unions_mult = 0.25
	}
	institution = institution_corporate_unions
	institution_modifier = {
		interest_group_ig_industrialists_pol_str_mult = 0.1
		interest_group_ig_armed_forces_pol_str_mult = 0.05
		country_radicals_from_legitimacy_mult = 0.1
		building_minimum_wage_mult = -0.05
		state_birth_rate_mult = -0.01
		building_group_bg_manufacturing_throughput_add = 0.05
		building_group_bg_mining_throughput_add = 0.05
	}

	ai_will_do = {
		any_interest_group = {
			is_in_government = yes
			leader ?= {
				has_ideology = ideology:ideology_monopolist_leader
			}
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

law_equity_compensation = {
	group = lawgroup_labour_associations
	parent = law_right_to_associate
	icon = "gfx/interface/icons/law_icons/unused/no_income_tax.dds"

	progressiveness = 75

	is_visible = {
		has_law_or_variant = law_type:law_directorate
	}

	unlocking_laws = {
		law_sovereign_monopoly
	}

	can_enact = {
		OR = {
			is_revolutionary = yes
			any_interest_group = {
				is_insurrectionary = yes
				law_stance = {
					law = law_type:law_right_to_associate
					value > neutral
				}
			}
		}
	}

	on_enact = {
		if = {
			limit = {
				NOT = {
					has_law_or_variant = law_type:law_corporatized_unions
				}
			}
			custom_tooltip = trade_unions_more_corporatist_effect_tt
		}
		if = {
			limit = {
				OR = {
					has_law_or_variant = law_type:law_combination_acts
					has_law_or_variant = law_type:law_national_labor_service
				}
			}
			custom_tooltip = trade_unions_much_less_radical_effect_tt
		}
	}

	modifier = {
		country_authority_mult = -0.1
		building_company_worker_dividends_add = 0.2
		building_company_government_dividends_add = -0.2
		building_group_bg_manufacturing_allowed_collectivization_add = 0.25
		building_group_bg_extraction_allowed_collectivization_add = 0.25
		building_group_bg_power_allowed_collectivization_add = 0.25
		building_group_bg_private_infrastructure_allowed_collectivization_add = 0.25
		building_group_bg_arts_allowed_collectivization_add = 0.25
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
		if = {
			limit = {
				has_law_or_variant = law_type:law_council_republic
				any_scope_politician = {
					interest_group = {
						is_in_government = yes
					}
					has_ideology = ideology:ideology_anarchist
				}
			}
			add = 500
		}
	}
}

REPLACE:law_sul_party_unions = {
	group = lawgroup_labour_associations

	icon = "gfx/interface/icons/law_icons/party_unions.dds"

	parent = law_corporatized_unions

	progressiveness = 25

	is_visible = {
		has_law_or_variant = law_type:law_council_republic
	}

	unlocking_laws = {
		law_council_republic
	}

	disallowing_laws = {
		law_anarchy
	}

	on_enact = {
		custom_tooltip = sul_trade_unions_vanguardist_communist
		if = {
			limit = {
				has_law_or_variant = law_type:law_corporatized_unions
			}
			custom_tooltip = trade_unions_less_corporatist_effect_tt
		}
	}

	modifier = {
		country_institution_corporate_unions_max_investment_add = 2
		state_pop_support_movement_radical_mult = -0.25
		state_expected_sol_from_literacy = 2
		country_authority_add = 100
	}

	institution = institution_corporate_unions
	institution_modifier = {
		state_pop_support_movement_communist_mult = 0.1
		state_radicals_and_loyalists_from_sol_change_mult = 0.05
		interest_group_in_government_attraction_mult = 0.05
		country_loyalists_from_legitimacy_mult = 0.05
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}

		if = {
			limit = {
				has_law_or_variant = law_type:law_council_republic
				any_scope_politician = {
					interest_group = {
						is_in_government = yes
					}
					has_ideology = ideology:ideology_vanguardist
				}
			}
			add = 500
		}
	}
}

REPLACE:law_sul_syndicalism = {
	group = lawgroup_labour_associations

	icon = "gfx/interface/icons/law_icons/syndicalism.dds"

	parent = law_factory_councils

	progressiveness = 100

	is_visible = {
		has_law_or_variant = law_type:law_council_republic
	}

	unlocking_laws = {
		law_council_republic
	}
	
	can_enact = {
		has_law_or_variant = law_type:law_cooperative_ownership
		OR = {
			has_law_or_variant = law_type:law_anarchy
			country_has_voting_franchise = yes
		}
	}

	on_enact = {

		custom_tooltip = sul_trade_unions_anarchist_communist

		if = {
			limit = {
					has_law_or_variant = law_type:law_corporatized_unions
			}
			custom_tooltip = trade_unions_less_corporatist_effect_tt
		}
	}

	modifier = {
		interest_group_ig_trade_unions_pol_str_mult = 0.25
		state_loyalists_from_political_movements_mult = 0.25
		state_pop_support_movement_socialist_mult = 0.25
		state_pop_support_movement_anarchist_mult = 0.25
		building_group_bg_manufacturing_allowed_collectivization_add = 0.5
		building_group_bg_extraction_allowed_collectivization_add = 0.5
		building_group_bg_power_allowed_collectivization_add = 0.5
		building_group_bg_private_infrastructure_allowed_collectivization_add = 0.5
		building_group_bg_arts_allowed_collectivization_add = 0.5
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
		if = {
			limit = {
				has_law_or_variant = law_type:law_council_republic
				any_scope_politician = {
					interest_group = {
						is_in_government = yes
					}
					has_ideology = ideology:ideology_anarchist
				}
			}
			add = 500
		}
	}
}