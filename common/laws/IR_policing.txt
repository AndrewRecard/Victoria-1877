INJECT:law_no_police = {
	modifier = {
		interest_group_ig_rural_folk_pol_str_mult = 0.25
		country_radicals_from_legitimacy_mult = -0.15
		state_lower_strata_expected_sol_add = -2
		state_upper_strata_expected_sol_add = 2
	}
	on_enact = {
		if = {
			limit = {
				has_law_or_variant = law_type:law_directorate
				has_law_or_variant = law_type:law_private_defense_agencies
			}
			custom_tooltip = defense_contractors_molinarism_tt
			if = {
				limit = {
						has_law_or_variant = law_type:law_dedicated_police
				}
				custom_tooltip = defense_contractors_minarchism_negative_tt
			}else_if = {
				limit = {
					has_law_or_variant = law_type:law_militarized_police
				}
				custom_tooltip = defense_contractors_monopolism_negative_tt
			}
		}
	}
}

INJECT:law_local_police = {
	modifier = {
		country_institution_cost_institution_police_mult = -0.5
	}
	institution_modifier = {
		state_turmoil_effects_mult = -0.05
		state_mortality_turmoil_mult = 0.004
		building_minimum_wage_mult = -0.03
		state_radicalism_increases_violent_hostility_mult = -0.05
		state_radicalism_increases_cultural_erasure_mult = -0.05
		state_radicalism_increases_open_prejudice_mult = -0.05
		interest_group_ig_industrialists_pol_str_mult = 0.05
	}
	on_enact = {
		if = {
			limit = {
				has_law_or_variant = law_type:law_directorate
				has_law_or_variant = law_type:law_private_defense_agencies
			}
			custom_tooltip = defense_contractors_molinarism_tt
			if = {
				limit = {
					has_law_or_variant = law_type:law_dedicated_police
				}
				custom_tooltip = defense_contractors_minarchism_negative_tt
			}else_if = {
				limit = {
					has_law_or_variant = law_type:law_militarized_police
				}
				custom_tooltip = defense_contractors_monopolism_negative_tt
			}
		}
	}
}

INJECT:law_dedicated_police = {
	disallowing_laws = {
		law_anarchy
		law_polycentric_law
	}
	modifier = {
		country_institution_police_max_investment_add = -2
		country_authority_add = 100
	}
	institution_modifier = {
		state_turmoil_effects_mult = -0.08
		state_mortality_turmoil_mult = 0.001
		interest_group_ig_petty_bourgeoisie_pol_str_mult = 0.05
		state_loyalism_increases_full_acceptance_mult = 0.03
		country_loyalists_from_legitimacy_mult = 0.05
		state_radicals_from_political_movements_mult = 0.05
	}
	on_enact = {
		if = {
			limit = {
				has_law_or_variant = law_type:law_directorate
				has_law_or_variant = law_type:law_private_defense_agencies
			}
			custom_tooltip = defense_contractors_minarchism_tt
			if = {
				limit = {
					OR = {
						has_law_or_variant = law_type:law_no_police
						has_law_or_variant = law_type:law_local_police
					}
				}
				custom_tooltip = defense_contractors_molinarism_negative_tt
			}else_if = {
				limit = {
					has_law_or_variant = law_type:law_militarized_police
				}
				custom_tooltip = defense_contractors_monopolism_negative_tt
			}
		}
	}
}

REPLACE:law_militarized_police = {
	group = lawgroup_policing

	icon = "gfx/interface/icons/law_icons/militarized_police.dds"

	progressiveness = -100

	unlocking_technologies = {
		central_planning
	}
	
	disallowing_laws = {
		law_anarchy
		law_polycentric_law
	}

	can_enact = {
		custom_tooltip = {
			text = no_private_defense_agencies_tt
			OR = {
				modifier:country_public_institutions_liquidated_bool = no
				has_law_or_variant = law_type:law_private_defense_agencies
			}
		}
	}
	
	modifier = {
		country_institution_cost_institution_police_mult = 0.25
		unit_experience_gain_mult = 0.25
		country_military_goods_cost_mult = 0.1
		country_military_wages_mult = 0.15
		country_authority_add = 200
	}

	institution = institution_police
	institution_modifier = {
		state_turmoil_effects_mult = -0.12
		state_radicals_and_loyalists_from_sol_change_mult = -0.05
		country_loyalists_from_legitimacy_mult = -0.05
		country_radicals_from_legitimacy_mult = -0.05
		interest_group_ig_armed_forces_pol_str_mult = 0.10
		state_mortality_turmoil_mult = 0.003
	}
	on_enact = {
		if = {
			limit = {
				has_law_or_variant = law_type:law_directorate
				has_law_or_variant = law_type:law_private_defense_agencies
			}
			custom_tooltip = defense_contractors_monopolism_tt
			if = {
				limit = {
					OR = {
						has_law_or_variant = law_type:law_no_police
						has_law_or_variant = law_type:law_local_police
					}
				}
				custom_tooltip = defense_contractors_molinarism_negative_tt
			}else_if = {
				limit = {
					has_law_or_variant = law_type:law_dedicated_police
				}
				custom_tooltip = defense_contractors_minarchism_negative_tt
			}
		}
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_policing_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = {
				has_law_or_variant = law_type:law_no_police
			}
			add = 500
		}

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_militarized_police
				power_bloc ?= { has_principle_group = principle_group:principle_group_police_coordination }
			}
			add = base_impose_law_weight
		}

		if = {
			limit = {
				has_law = law_type:law_militarized_police
				scope:target_country = {
					has_law_or_variant = law_type:law_no_police
				}
				power_bloc ?= { has_principle_group = principle_group:principle_group_police_coordination }
			}
			add = 10
		}

		if = {
			limit = {
				scope:target_country = {
					c:BIC ?= this
					OR = {
						has_journal_entry = je_british_dictate_plantations
						has_journal_entry = je_british_dictate_military
						has_journal_entry = je_british_dictate_universities
					}
				}
			}
			multiply = 0
		}
	}
}
