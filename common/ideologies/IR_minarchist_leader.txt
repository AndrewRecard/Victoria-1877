ideology_minarchist_leader = {
	icon = "gfx/interface/icons/ideology_icons/ideology_leader/unused/ideology_leader_isolationist.dds"

	character_ideology = yes

	lawgroup_governance_principles = {
		law_monarchy = disapprove
		law_theocracy = strongly_disapprove
		law_council_republic = strongly_disapprove
		law_presidential_republic = disapprove
		law_parliamentary_republic = disapprove
		law_corporate_state = disapprove
		law_directorate = approve
	}

	lawgroup_distribution_of_power = {
		law_autocracy = disapprove
		law_oligarchy = approve
		law_technocracy = disapprove
		law_landed_voting = neutral
		law_wealth_voting = approve
		law_universal_suffrage = strongly_disapprove
		law_census_voting = strongly_disapprove
		law_anarchy = disapprove
		law_single_party_state = strongly_disapprove
	}

	lawgroup_bureaucracy = {
		law_hereditary_bureaucrats = disapprove
		law_appointed_bureaucrats = disapprove
		law_elected_bureaucrats = disapprove
		law_private_administration = approve
	}
	
	lawgroup_economic_system = {
		law_cooperative_ownership = strongly_disapprove
		law_command_economy = disapprove
		law_interventionism = neutral
		law_agrarianism = disapprove
		law_traditionalism = strongly_disapprove
		law_laissez_faire = approve
		law_industry_banned = strongly_disapprove
		law_extraction_economy = disapprove
	}

	lawgroup_health_system = {
		law_no_health_system = neutral
		law_private_health_insurance = approve
		law_public_health_insurance = strongly_disapprove
		law_charitable_health_system = neutral
	}

	lawgroup_education_system = {
		law_no_schools = neutral
		law_religious_schools = disapprove
		law_public_schools = disapprove
		law_private_schools = approve
	}

	lawgroup_army_model = {
		law_peasant_levies = neutral
		law_national_militia = neutral
		law_professional_army = neutral
		law_mass_conscription = disapprove
		law_private_defense_agencies = approve
	}
	
	lawgroup_policing = {
		law_local_police = neutral
		law_dedicated_police = approve
		law_militarized_police = disapprove
		law_no_police = disapprove
	}

	lawgroup_internal_security = {
		law_guaranteed_liberties = strongly_disapprove
		law_no_home_affairs = neutral
		law_national_guard = neutral
		law_secret_police = approve
		law_ministry_of_truth = disapprove
	}

	country_trigger = {
		has_technology_researched = shareholder_primacy
	}

	interest_group_leader_trigger = {
		ideology_minarchist_valid_trigger = yes
	}

	non_interest_group_leader_trigger = {
		NAND = {
			has_role = agitator
			owner = {
				NOT = {
					boardroom_schism_resolved_trigger = yes
				}
				OR = {
					has_law = law_type:law_oligopoly
					has_law = law_type:law_managed_democracy
				}
			}
		}
		interest_group = {
			ideology_minarchist_valid_trigger = yes
		}
	}

	interest_group_leader_weight = {
		value = 100

		if = {
			limit = {
				owner.ideology_minarchist_leader_disfavoured_laws > 0
			}
			add = {
				value = owner.ideology_minarchist_leader_disfavoured_laws
				multiply = 7  # less ideological, more profit driven
			}
		}

		if = {
			limit = {
				owner.ideology_minarchist_leader_favoured_laws > 0
			}
			subtract = {
				value = owner.ideology_minarchist_leader_favoured_laws
				multiply = 7
			}
		}

		# more likely if bankrupt
		if = {
			limit = {
				owner = {
					NOT = {
						has_law_or_variant = law_type:law_directorate
					}
					OR = {
						in_default = yes
						has_modifier = declared_bankruptcy
					}
				}
			}
			add = 25
		}
		# Less likely in council republic
		if = {
			limit = {
				owner = {
					has_law_or_variant = law_type:law_council_republic
				}
			}
			add = -75
		}
		# Less likely in theocracy
		if = {
			limit = {
				owner = {
					has_law_or_variant = law_type:law_theocracy
				}
			}
			add = -50
		}
		# Less likely in corporate state due to facilitated interaction between classes
		if = {
			limit = {
				owner = {
					has_law_or_variant = law_type:law_corporate_state
				}
			}
			add = -25
		}
		# More likely in presidential republic due to polarisation
		if = {
			limit = {
				owner = {
					has_law_or_variant = law_type:law_presidential_republic
				}
			}
			add = 15
		}
		# more likely if angry (non directorate)
		if = {
			limit = {
				ig_approval <= -5
				owner = {
					NOR = {
						has_law_or_variant = law_type:law_directorate
						has_law = law_type:law_oligopoly
						has_law = law_type:law_managed_democracy
					}
				}
			}
			subtract = {
				value = ig_approval
				multiply = 5
			}
		}
		# even more likely if angry under powerful trade unions or rivals
		if = {
			limit = {
				ig_approval <= -5
				owner = {
					any_interest_group = {
						OR = {
						    is_interest_group_type = ig_trade_unions
							leader = {
								OR = {
									has_ideology = ideology:ideology_monopolist_leader
									has_ideology = ideology:ideology_molinarist_leader
								}
							}
						}
						is_powerful = yes
						is_in_government = yes
					}
				}
			}
			subtract = {
				value = ig_approval
				multiply = 5
			}
		}
		# less likely if content without moderate directorate
		if = {
			limit = {
				owner = {
					has_journal_entry = je_boardroom_schism
					NOR = {
						has_law = law_type:law_oligopoly
						has_law = law_type:law_managed_democracy
					}
				}
				ig_approval >= 5
			}
			subtract = {
				value = ig_approval
				multiply = 5
			}
		}
		# more likely if content with moderate directorate
		if = {
			limit = {
				owner = {
					has_law_or_variant = law_type:law_directorate
					OR = {
						has_law_or_variant = law_type:law_oligarchy
						has_law_or_variant = law_type:law_wealth_voting
					}
				}
				ig_approval >= 5
			}
			add = {
				value = ig_approval
				multiply = 5
			}
		}
		# more likely for industrialists
		if = {
			limit = {
				is_interest_group_type = ig_industrialists
			}
			add = 50
		}
		# more likely for landowners with radical land reforms
		if = {
			limit = {
				is_interest_group_type = ig_landowners
				owner = {
					NOT = {
						has_law_or_variant = law_type:law_directorate
					}
					OR = {
						has_law_or_variant = law_type:law_homesteading
						has_law_or_variant = law_type:law_collectivized_agriculture
					}
				}
			}
			add = 25
		}
		# more likely for intelligentsia in laissez-faire economic system
		if = {
			limit = {
				is_interest_group_type = ig_intelligentsia
				owner = {
					NOT = {
						has_law_or_variant = law_type:law_directorate
					}
					has_law_or_variant = law_type:law_free_trade
					has_law_or_variant = law_type:law_laissez_faire
				}
			}
			add = 25
		}
		# more likely for rural folk owning property in laissez-faire system
		if = {
			limit = {
				is_interest_group_type = ig_rural_folk
				owner = {
					has_law_or_variant = law_type:law_homesteading
					OR = {
						has_law_or_variant = law_type:law_laissez_faire
						has_law_or_variant = law_type:law_free_trade
					}
				}
			}
			add = 50 # cut in half when socialism
		}
		# less likely for PB with fascist movement
		if = {
			limit = {
				is_interest_group_type = ig_petty_bourgeoisie
				owner = {
					any_political_movement = {
						is_political_movement_type = movement_fascist
					}
				}
			}
			add = -25
		}
		# less likely for rural folk with socialism
		if = {
			limit = {
				is_interest_group_type = ig_rural_folk
				owner = {
					has_technology_researched = socialism
				}
			}
			add = -25
		}
		# much more likely for defense contractors in general (without directorate)
		if = {
			limit = {
				is_interest_group_type = ig_armed_forces
				has_ideology = ideology:ideology_martial_corporatist
				owner = {
					NOT = {
						has_law_or_variant = law_type:law_directorate
					}
				}
			}
			add = 50
		}
		# less likely for non private defense agencies
		if = {
			limit = {
				is_interest_group_type = ig_armed_forces
				NOT = {
					has_ideology = ideology:ideology_martial_corporatist
				}
			}
			add = -25
		}
		# more likely if in government with directorate/oligarchy/wealth voting
		if = {
			limit = {
				owner = {
					has_law_or_variant = law_type:law_directorate
					OR = {
						has_law_or_variant = law_type:law_wealth_voting
						has_law_or_variant = law_type:law_oligarchy
					}
				}
				is_in_government = yes
			}
			add = 50
		}
		# more likely for defense contractors with dedicated police (in directorate)
		if = {
			limit = {
				is_interest_group_type = ig_armed_forces
				has_ideology = ideology:ideology_martial_corporatist
				owner = {
					has_law_or_variant = law_type:law_dedicated_police
					has_law_or_variant = law_type:law_directorate
				}
			}
			multiply = 1.5
		}
		# more likely if supporting
		if = {
			limit = {
				owner = {
					has_journal_entry = je_boardroom_schism
					has_variable = supporting_minarchism_var
				}
			}
			multiply = 1.25
		}
		# propertarian JE causes an uptick in propertarianism
		if = {
			limit = {
				owner = {
					OR = {
						has_journal_entry = je_industry_and_integrity
						has_variable = completed_industry_and_integrity
					}
				}
			}
			multiply = 1.25
		}
		if = {
			limit = {
				owner = {
					minarchists_failed_schism_trigger = yes
				}
			}
			multiply = 0.5
		}
		if = {
			limit = {
				OR = {
					has_variable = communist_ig_var
					has_variable = chose_vanguardism
					has_variable = chose_anarchism
					has_variable = chose_communism
					has_variable = chose_ethno_nationalism
					has_variable = chose_fascism
				}
			}
			multiply = 0.1
		}
	}

	non_interest_group_leader_weight = {
		value = 100

		if = {
			limit = {
				owner.ideology_minarchist_leader_disfavoured_laws > 0
			}
			add = {
				value = owner.ideology_minarchist_leader_disfavoured_laws
				multiply = 7  # less ideological, more profit driven
			}
		}

		if = {
			limit = {
				owner.ideology_minarchist_leader_favoured_laws > 0
			}
			subtract = {
				value = owner.ideology_minarchist_leader_favoured_laws
				multiply = 7
			}
		}

		# more likely if bankrupt
		if = {
			limit = {
				owner = {
					NOT = {
						has_law_or_variant = law_type:law_directorate
					}
					OR = {
						in_default = yes
						has_modifier = declared_bankruptcy
					}
				}
			}
			add = 25
		}
		# Less likely in council republic
		if = {
			limit = {
				owner = {
					has_law_or_variant = law_type:law_council_republic
				}
			}
			add = -75
		}
		# Less likely in theocracy
		if = {
			limit = {
				owner = {
					has_law_or_variant = law_type:law_theocracy
				}
			}
			add = -50
		}
		# Less likely in corporate state due to facilitated interaction between classes
		if = {
			limit = {
				owner = {
					has_law_or_variant = law_type:law_corporate_state
				}
			}
			add = -25
		}
		# More likely in presidential republic due to polarisation
		if = {
			limit = {
				owner = {
					has_law_or_variant = law_type:law_presidential_republic
				}
			}
			add = 15
		}
		# more likely if angry (non directorate)
		if = {
			limit = {
				interest_group.ig_approval <= -5
				owner = {
					NOR = {
						has_law_or_variant = law_type:law_directorate
						has_law = law_type:law_oligopoly
						has_law = law_type:law_managed_democracy
					}
				}
			}
			subtract = {
				value = interest_group.ig_approval
				multiply = 5
			}
		}
		# even more likely if angry under powerful trade unions or rivals
		if = {
			limit = {
				interest_group.ig_approval <= -5
				owner = {
					any_interest_group = {
						OR = {
						    is_interest_group_type = ig_trade_unions
							leader = {
								OR = {
									has_ideology = ideology:ideology_monopolist_leader
									has_ideology = ideology:ideology_molinarist_leader
								}
							}
						}
						is_powerful = yes
						is_in_government = yes
					}
				}
			}
			subtract = {
				value = interest_group.ig_approval
				multiply = 5
			}
		}
		# less likely if content without moderate directorate
		if = {
			limit = {
				owner = {
					has_journal_entry = je_boardroom_schism
					NOR = {
						has_law = law_type:law_oligopoly
						has_law = law_type:law_managed_democracy
					}
				}
				interest_group.ig_approval >= 5
			}
			subtract = {
				value = interest_group.ig_approval
				multiply = 5
			}
		}
		# more likely if content with moderate directorate
		if = {
			limit = {
				owner = {
					has_law_or_variant = law_type:law_directorate
					OR = {
						has_law_or_variant = law_type:law_oligarchy
						has_law_or_variant = law_type:law_wealth_voting
					}
				}
				interest_group.ig_approval >= 5
			}
			add = {
				value = interest_group.ig_approval
				multiply = 5
			}
		}
		# more likely for industrialists
		if = {
			limit = {
				is_interest_group_type = ig_industrialists
			}
			add = 50
		}
		# more likely for landowners with radical land reforms
		if = {
			limit = {
				is_interest_group_type = ig_landowners
				owner = {
					NOT = {
						has_law_or_variant = law_type:law_directorate
					}
					OR = {
						has_law_or_variant = law_type:law_homesteading
						has_law_or_variant = law_type:law_collectivized_agriculture
					}
				}
			}
			add = 25
		}
		# more likely for intelligentsia in laissez-faire economic system
		if = {
			limit = {
				is_interest_group_type = ig_intelligentsia
				owner = {
					NOT = {
						has_law_or_variant = law_type:law_directorate
					}
					has_law_or_variant = law_type:law_free_trade
					has_law_or_variant = law_type:law_laissez_faire
				}
			}
			add = 25
		}
		# more likely for rural folk owning property in laissez-faire system
		if = {
			limit = {
				is_interest_group_type = ig_rural_folk
				owner = {
					has_law_or_variant = law_type:law_homesteading
					OR = {
						has_law_or_variant = law_type:law_laissez_faire
						has_law_or_variant = law_type:law_free_trade
					}
				}
			}
			add = 50 # cut in half when socialism
		}
		# less likely for PB with fascist movement
		if = {
			limit = {
				is_interest_group_type = ig_petty_bourgeoisie
				owner = {
					any_political_movement = {
						is_political_movement_type = movement_fascist
					}
				}
			}
			add = -25
		}
		# less likely for rural folk with socialism
		if = {
			limit = {
				is_interest_group_type = ig_rural_folk
				owner = {
					has_technology_researched = socialism
				}
			}
			add = -25
		}
		# much more likely for defense contractors in general (without directorate)
		if = {
			limit = {
				is_interest_group_type = ig_armed_forces
				interest_group = {
					has_ideology = ideology:ideology_martial_corporatist
				}
				owner = {
					NOT = {
						has_law_or_variant = law_type:law_directorate
					}
				}
			}
			add = 50
		}
		# less likely for non private defense agencies
		if = {
			limit = {
				is_interest_group_type = ig_armed_forces
				NOT = {
					interest_group = {
						has_ideology = ideology:ideology_martial_corporatist
					}
				}
			}
			add = -25
		}
		# more likely if in government with directorate/oligarchy/wealth voting
		if = {
			limit = {
				owner = {
					has_law_or_variant = law_type:law_directorate
					OR = {
						has_law_or_variant = law_type:law_wealth_voting
						has_law_or_variant = law_type:law_oligarchy
					}
				}
				interest_group = {
					is_in_government = yes
				}
			}
			add = 50
		}
		# more likely for defense contractors with dedicated police (in directorate)
		if = {
			limit = {
				is_interest_group_type = ig_armed_forces
				interest_group = {
					has_ideology = ideology:ideology_martial_corporatist
				}
				owner = {
					has_law_or_variant = law_type:law_dedicated_police
					has_law_or_variant = law_type:law_directorate
				}
			}
			multiply = 1.5
		}
		# more likely if supporting
		if = {
			limit = {
				owner = {
					has_journal_entry = je_boardroom_schism
					has_variable = supporting_minarchism_var
				}
			}
			multiply = 1.25
		}
		# propertarian JE causes an uptick in propertarianism
		if = {
			limit = {
				owner = {
					OR = {
						has_journal_entry = je_industry_and_integrity
						has_variable = completed_industry_and_integrity
					}
				}
			}
			multiply = 1.25
		}
		if = {
			limit = {
				owner = {
					minarchists_failed_schism_trigger = yes
				}
			}
			multiply = 0.5
		}
		if = {
			limit = {
				interest_group = {
					OR = {
						has_variable = communist_ig_var
						has_variable = chose_vanguardism
						has_variable = chose_anarchism
						has_variable = chose_communism
						has_variable = chose_ethno_nationalism
						has_variable = chose_fascism
					}
				}
			}
			multiply = 0.1
		}
	}
}